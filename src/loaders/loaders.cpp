#include "loaders/loaders.h"
#include <armadillo>

using namespace arma;
using namespace std;

mat loaders::load_csv(string path) {
    cout << "I ran!\n";
    mat ret(3, 3, fill::zeros);
    return ret;
}